# Frontend → FastAPI (no trailing slash)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# If the UI runs on another port (e.g. 3000), add that exact origin to backend CORS_ORIGINS
# in backend/.env (see backend/.env.example).
